interface:
  display_name: "Init Project Wiki"
  short_description: "Initialize strict code-grounded .spwiki knowledge bases"
  default_prompt: "Use $simple-project-wiki-init to initialize a code-grounded .spwiki knowledge base, create sidecar config files, fill pages from source, rebuild the index, and run strict recursive readiness checks."
